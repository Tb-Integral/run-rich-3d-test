# Состояние проекта — 16.09.2026

## Исходное состояние
Unity 6000.3.22f1; URP 17.3.0; Input System 1.20.0; Test Framework 1.6.0. Единственная сцена — SampleScene. Git ещё не был инициализирован. Сериализация Force Text уже включена.
В Assets 3119 файлов вместе с .meta, около 217 МБ: 6 FBX, 7 shader, 74 mat, 225 PNG, 23 OGG, 4 C#; нет отдельных prefab, anim, controller. Исходная папка задания содержит такой же набор расширений.

## Подтверждённые проблемы
- LevelManager использовал UnityEditor без условной компиляции: ошибка при сборке Player.
- Singleton присваивался в конструкторе MonoBehaviour.
- CurrentLevel setter вызывал GetInt вместо записи; индексы смешивались с номером уровня.
- Пустой список и единственный случайный уровень приводили к недопустимым индексам/делению на ноль.
- Удаление детей использовало DestroyImmediate в runtime.
- Inspector удалял все PlayerPrefs и посторонние файлы сохранений.
- Выданные surface shaders содержат DummyShaderTextExporter и не совместимы с URP; восстановление внешнего вида нужно в этапе 1.
- Игровая сцена, готовые персонажи и анимационные контроллеры ещё не собраны. Это исходная незавершённость, а не готовый первый уровень.

## Выполненные исправления
- Инициализация singleton перенесена в Awake, ссылка очищается при уничтожении менеджера.
- Разделены порядковый номер прохождения и индекс префаба; исправлены сохранение прогресса, повтор одного уровня и нормализация индексов.
- Добавлены проверки пустого списка и отсутствующего префаба; неудачный выбор сохраняет текущий уровень и прогресс.
- Старый уровень сразу отключается и уничтожается отложенно в Play Mode.
- Сброс очищает только четыре ключа Level Manager; удаление посторонних сохранений исключено.
- Runtime и Editor отделены сборками, добавлены девять PlayMode-тестов и Editor-инструмент диагностики.
- Подготовлены AGENTS.md, два локальных скилла, две конфигурации ролей ревью, правила совместимости Cursor, настройки текста и Git.

## Проверки подготовительного этапа
Проверено в отдельной копии Assets, Packages и ProjectSettings, с чистым импортом Unity 6000.3.22f1.
- Компиляция Editor: успешно.
- PlayMode: **9 из 9 тестов пройдены**, ошибок тестов нет.
- Отсутствующие MonoBehaviour в сценах сборки: **0**.
- Материалы без доступного шейдера: **42**. Они остаются исходными ресурсами и требуют восстановления в этапе 1; проверка проекта не означает готовность этих материалов.
- FBX player: **85 Transform, 8 SkinnedMeshRenderer** — скелет и меши есть.
- FBX photographer: **81 Transform, 2 SkinnedMeshRenderer**.
- Анимационные клипы в шести FBX: **0**. Нужно создавать/подбирать и ретаргетировать анимации; это существенная работа, а не только настройка Animator.
- Все 7 поставленных шейдеров — surface shaders; их нельзя напрямую использовать для целевого вида в URP.
- Скиллы прошли quick_validate; TOML-конфигурации агентов разбираются корректно. Оба скилла обнаружены Codex; отдельный запуск ролей ревью не выполнялся.
- Проверочная **Windows x64 Development-сборка исходной SampleScene успешна: 0 ошибок, 0 предупреждений BuildReport**, код выхода Unity 0. Она не проверяет внешний вид всех неиспользованных материалов.
- Первый запуск сборки завершился OutOfMemoryException в Burst при компиляции URP. Повтор успешно завершён с `-job-worker-count 2 -background-job-worker-count 2` и ограничением процесса двумя логическими CPU (ProcessorAffinity=3). Burst и оптимизации не отключались, пакеты не обновлялись. При повторении нехватки памяти сократить параллельность сборки или освободить память.
- Проверенные C# и asmdef побайтово совпадают с исходным проектом. Временная копия и логи остаются локально в Temp/ValidationProject.
- Геймплей и визуальное соответствие ещё не проверяются: игровая сцена не реализована.

## Источники технических решений
- [Surface shaders и совместимость с URP](https://docs.unity3d.com/6000.3/Documentation/Manual/SL-SurfaceShaders.html).
- [Запуск Unity из командной строки](https://docs.unity3d.com/6000.3/Documentation/Manual/EditorCommandLineArguments.html).
- [Локальные скиллы Codex](https://learn.chatgpt.com/docs/build-skills).
- [Конфигурации агентов Codex](https://learn.chatgpt.com/docs/agent-configuration/subagents).

## Ограничения исходных ассетов
Найдены ссылки GUID, отсутствующие среди локальных ассетов и файлов пакетов. Это отдельная проблема поставки, требующая восстановления назначений материалов и UI при сборке этапа 1. Не следует массово заменять GUID догадками. Результаты загрузки материалов и FBX приведены выше.
Пользователь подтвердил: дополнительных архивов с анимациями и персонажами нет.

## Этап 1 — первая версия визуальной основы

- Добавлены Gameplay, редактируемый префаб Runner и профиль RunRichPipeline. В сцене используется один исходный костюм `casual`, текстуры `atlas.png` и `Water.png`.
- Созданы URP-шейдеры/материалы персонажа, дорожки, воды и неба. Материалы имеют общую разметку констант между проходами шейдера; тени проверены на рендере.
- Добавлены обычные сохранённые клипы ожидания и ходьбы. Расчёт поз применяется только Editor-инструментом при явной подготовке ассетов, во время игры работает Animator.
- Восстановлен официальный TMP Essential Resources из установленного пакета Unity UI. Это вернуло отсутствующий шейдер встроенному материалу исходного шрифтового ассета.
- Для оставшихся материалов применён реестр преобразований Render Pipeline Converter (`MaterialUpgrader`). Поддерживаемых преобразований не найдено: **0 конвертированных материалов**. Причины повреждений — отсутствующие шейдеры/текстуры и заглушки surface shaders.
- По разрешению пользователя удалены **66 неиспользуемых сломанных материалов** вместе с их `.meta`. Unity не нашла входящих зависимостей; дополнительный поиск GUID в сериализованных файлах также не нашёл ссылок. [Полный список удалённых материалов](MATERIAL_RECOVERY_STAGE1.json).
- Сохранены исходные модели, текстуры, шрифты и оставшиеся материалы. Выданный Level Manager на этом этапе не изменялся.

### Проверка этапа 1

- Отдельная проверочная копия теперь находится в `Logs/ValidationProject`: Unity очищает собственную папку Temp при перезапуске, поэтому она непригодна для сохранения результатов между сессиями.
- Компиляция Editor и генерация сцены выполнены без ошибок C# и шейдеров.
- **10/10 PlayMode-тестов пройдены**: девять проверок Level Manager и проверка анимации Gameplay, единственного активного костюма, назначенных материалов и отсутствия root motion.
- **Windows x64 Development-сборка Gameplay успешна: 0 ошибок, 0 предупреждений BuildReport**, код выхода Unity 0. Использованы два логических CPU и два job worker; Burst включён.
- Аудит сцен сборки: **0 отсутствующих компонентов**, аудит материалов: **0 материалов без доступного шейдера**. Найдены два созданных `.anim`; в исходных FBX клипов по-прежнему нет.
- Просмотрены рендеры 880×1920: ожидание и две фазы ходьбы. Снимки снимаются на разных обновлениях Editor, чтобы данные skinned mesh успели обновиться между позами. Локальные изображения — `Logs/Stage1`.
- Исходники, новые ассеты и пакеты в основном проекте и проверочной копии побайтово сопоставлены. Зависимости не обновлялись.
- Дубликатов GUID не найдено; у новых ресурсов `_Project` присутствуют `.meta`. Проверка `git diff --check` пройдена.
- Ручная визуальная приёмка пользователем ожидается. Анимации остаются приближением по видео; совпадение 1:1 не заявляется. Проверка материалов текущей сцены не означает готовность всех исходных UI-ресурсов и будущих объектов уровня.

## Доработка этапа 1 по замечаниям пользователя

- Исправлены ступенчатые тени: карта 4096, четыре каскада на дистанции 28, мягкая фильтрация High. В собственный шейдер добавлены варианты качества мягких теней URP. Проверены новые рендеры, контуры сглажены.
- Восстановлена связь сцены с исходным model prefab `player.fbx`: модель находится внутри `Runner/VisualPivot`, остаётся связанной с FBX. Удалены распакованный Runner.prefab, временные CasualIdle/CasualWalk и их генератор.
- Настроены Humanoid Avatar для исходных player/photographer и семи FBX-анимаций пользователя. Файлы моделей не заменены, GUID сохранены; изменены настройки импорта `.meta` для ретаргетинга. Все Avatar успешно прошли проверку `isValid && isHuman`.
- К скачанным клипам настроены повторение и запекание root motion. Stomping не зациклен. Контроллер игрока использует Look Around, три походки, танец, поражение и созданный UpgradePose. Подготовлен отдельный Photographer.controller с Arm Stretching; фотограф в Gameplay пока не размещён.
- Добавлено непрерывное смешивание Sad Walk → Walking → Happy Walk по параметру Happiness 0–1, со сглаживанием при смене значения.
- Добавлен поворот визуала на 360° за 0.65 с только при улучшении одежды. Позу с разведёнными руками и согнутой ногой восстановили по скриншоту. Ухудшение не запускает и не перезапускает анимацию шага; при прерывании поворота возвращается обычная походка. Поражение прерывает поворот и запускает Stomping.
- Кнопки ручной проверки находятся в Editor Inspector; игровой UI и экономика пока не реализованы. Связь счастья с пикапами и воротами внесена в этапы 4–5.
- **11/11 PlayMode-тестов пройдены** на доработанной версии, включая сглаживание Happiness, улучшение/ухудшение, прерывание вращения, отключение/включение объекта и поражение. Аудит: **0 отсутствующих компонентов, 0 материалов без шейдера**.
- Повторная **Windows x64 Development-сборка доработанной Gameplay успешна: 0 ошибок и 0 предупреждений**, код выхода Unity 0. Отчёт сохранён локально в `Logs/revision-build-result.txt`.
- После сборки сопоставлены исходники и ассеты основной и проверочной копий. Изменившиеся поля `m_Prefilter*` профиля URP — автоматически рассчитанные параметры отбора вариантов шейдеров; итоговый профиль перенесён вместе с проверенными настройками теней. GUID исходных моделей сохранены.
- Просмотрены новые кадры ожидания и позы поворота 880×1920; локальные результаты сохранены в `Logs/Stage1-revision`. Движение и точность позы требуют повторной ручной приёмки.

Техническая основа ретаргетинга: [документация Unity](https://docs.unity.cn/6000.1/Documentation/Manual/Retargeting.html); настройки теней проверены по исходникам установленного URP 17.3.0.

## Ошибка импорта в открытом основном Editor

Пользователь сообщил CS0246/CS0103 для PlayerPresentation. Проверка основного Editor.log и списка исходников компиляции подтвердила: Unity считала RunRich.Runtime пустой сборкой, PlayerPresentation.cs отсутствовал в списке компиляции и Runtime.dll не создавалась. На диске сам класс и ссылки Editor/Tests на Runtime присутствуют. Проверки отдельной копии не выявляли это состояние открытого редактора.

В PlayerPresentation.cs.meta добавлена явная секция MonoImporter с сохранением GUID. После обновления пользователь смог запустить Play Mode; основной Editor пересобрал RunRich.Runtime, Editor и PlayModeTests 16.09.2026 в 10:56. Ошибка импорта устранена.

## Исправление обратного сгиба коленей

- Воспроизведён дефект: в Sad Walk колено выгибалось назад до 34.25°, в Walking — до 27.82°, в Happy Walk — до 28.65°.
- Причина: при создании отдельных Humanoid Avatar опорные позы брались из согнутых поз FBX-анимаций, а у модели использовалась T-поза. Поставленные клипы имеют те же кости и пропорции, но не содержат промежуточного узла Armature модели.
- Настройка импорта теперь сохраняет иерархию клипа и использует опорные положения, вращения и масштабы костей исходной модели. Простое Copy From Other не используется: иерархии различаются узлом Armature. Кривые скачанных анимаций не редактировались, runtime-ограничение коленей не добавлялось.
- Измерены по 120 поз каждого цикла на модели: после исправления минимальный сгиб вперёд составил 0.24° для Sad Walk, 12.36° для Walking и 1.37° для Happy Walk. Обратного сгиба в этих выборках нет.
- Просмотрены восемь фаз грустной походки сбоку до/после исправления. Диагностические изображения и измерения находятся локально в `Logs/ValidationProject/Logs/Gait`.
- Добавлена проверка обеих ног в PlayMode: 120 фаз при пяти значениях Happiness, включая смешанные походки. Поза улучшения одежды пересоздана из исправленной Walking.
- **12/12 PlayMode-тестов пройдены**, включая новую проверку коленей. Настройки импорта семи клипов и UpgradePose перенесены в основной проект, GUID сохранены. Сцена и runtime-код представления в этом исправлении не менялись.
- 16.09.2026 пользователь подтвердил, что результат исправлен, и разрешил коммит этапа 1. Ручная приёмка текущего этапа завершена; это не приёмка ещё не реализованных управления и игрового цикла.

## Этап 2 — техническая проверка

- Добавлены независимые компоненты ввода, движения, пути и камеры, общие сериализованные настройки и сохранённая дорожка с двумя поворотами. Исходные модели, импорт анимаций и Level Manager на этом этапе не изменялись.
- В отдельной копии проекта `Logs/ValidationProject` пройдены **18/18 PlayMode-тестов**. Проверены имитированные события мыши/touch, повторное нажатие, смена пальца, изменение ширины окна, края и обратное движение, остановка/конец пути/повтор, непрерывность поворотов и положение персонажа в кадре.
- Одинаковый жест рассчитан при 30/60/120 шагах в секунду и ширине 440/880. Камера проверена для 440:960 и 9:16. Это автоматические расчёты, не ручной тест реального устройства.
- Прежние проверки коленей, смешивания походок и костюмов сохранены. В изолированных тестах представления мотор отключён, чтобы не подменять проверку анимаций движением сцены.
- После обнаруженного выхода персонажа из кадра при резком жесте камера настроена на непосредственное следование за 80% бокового смещения; сглаживается поворот камеры.
- Просмотрены пять отрисованных кадров начала, прямых и обоих поворотов (`Logs/ValidationProject/Logs/Stage2`). Ручная оценка ощущения управления ожидается.
- Аудит: **0 отсутствующих компонентов, 0 материалов без шейдера**. **Windows x64 Development-сборка успешна: 0 ошибок, 0 предупреждений**, код выхода Unity 0.
- Отчёты: `Logs/ValidationProject/Logs/stage2-tests-final.xml`, `stage2-build.log`, `build-result.txt`. Сцена и созданные ассеты перенесены в основной проект с сохранением GUID. Открытый основной Editor и touch на телефоне требуют пользовательской проверки.

## Поворот модельки при боковом движении

- По уточнению пользователя сохранены перемещение и чувствительность; добавлен ограниченный плавный поворот VisualPivot по фактической боковой скорости. При неподвижном указателе, отпускании и упоре в край модель выпрямляется.
- Поворот не меняет корневую позицию, направление трассы или камеру. Вращение при улучшении одежды складывается с рулением; сброс, отключение и поражение очищают угол.
- Пройдены **19/19 PlayMode-тестов**, включая новую проверку левого/правого поворота, возврата прямо, неизменности корня, смены одежды и сброса. Результат: `Logs/ValidationProject/Logs/stage2-steering-final.xml`.
- Стабилизировано ожидание завершения перехода в Locomotion в прежнем тесте: проверяется фактическое состояние с ограничением времени вместо попадания на точную границу 0.8 с.
- Сборка Windows выше относится к предыдущей версии этапа 2; для этой доработки проверены компиляция и PlayMode. Визуальная ручная приёмка ожидается.

## Слабый визуальный отклик поворота

- Пользователь сообщил, что поворот не виден; подтвердил Max Steering Angle = 25. Проверка скомпилированного RunnerMotor в основном Editor подтвердила вызов SetMovementDirection.
- Временная диагностика основного Editor при пользовательском запуске зарегистрировала Walking, боковое смещение до 1.8 и ненулевой угол, включая -4.64°; наибольшее зафиксированное значение модуля угла при выборке раз в 0.2 с — 7.31°. Это измерение конкретного прохода, а не предел возможного угла.
- Усилен визуальный отклик на боковую скорость в 3 раза; время сглаживания начала поворота уменьшено с 0.1 до 0.025 с, возврат остался 0.1 с. Максимум — 25°. Перемещение и чувствительность ввода не менялись. Значения явно сохранены в Gameplay.
- Исправлен вызов CrossFade у неактивного Animator при выходе из Play Mode: состояние сохраняется и применяется в OnEnable. Предупреждение было подтверждено основным Editor.log.
- Пройдены **20/20 PlayMode-тестов**. Новая проверка подаёт события мыши перед игровым Update, проходит через RunnerDragInput → RunnerMotor → PlayerPresentation и проверяет фактическое вращение VisualPivot. При жесте получено смещение 0.851 и угол 25°, после отпускания модель выпрямилась. Отчёт: `Logs/ValidationProject/Logs/steering-response-final.xml`. Временная диагностика основного Editor удалена.

## Повторное замечание: около 7° после короткого жеста

- Предыдущее усиление скорости не решало однокадровый ввод. При 120 FPS первый шаг сглаживания равен `25 × (1 − exp(−(1/120)/0.025)) = 7.0867°`; следующий нулевой ввод уже возвращал модель прямо. Это воспроизводимый сценарий старой формулы, соответствующий замечанию пользователя; длительный жест из прежнего теста его не проверял.
- Убрана зависимость целевого визуального угла от отношения боковой и продольной скоростей. Фактическое боковое движение задаёт сторону ±25°, а задержка отпускания 0.08 с соединяет редкие события мыши. После паузы угол плавно возвращается к нулю. Перемещение, чувствительность ввода и камера сохранены.
- Настройки сохранены в Gameplay. Убрано устаревшее поле Steering Response; добавлено Steering Release Delay.
- Пройдены **21/21 PlayMode-тестов**. Новый тест одиночного события мыши через настоящий Update контроллера проверяет обе стороны и возврат без отпускания кнопки при 30/60/120 FPS. Пики: 23.26° при 30/60 FPS, 23.76° при 120 FPS; проверена нейтраль после отсутствия движения.
- Отчёт: `Logs/ValidationProject/Logs/steering-short-gesture.xml`. Это результат автоматического воспроизведения; окончательная визуальная приёмка в пользовательском Editor ожидается.

## Этап 3 — цикл забега и стартовый UI

- Этап 2 принят пользователем и опубликован в коммите `5089c1c`. Реализован отдельный этап 3; его ручная приёмка ожидается.
- Добавлены RunSession, RunLevelSource и RunHud; дорожка перенесена в Level01.prefab, подключён имеющийся Level Manager. Исходный код Level Manager не изменялся.
- Готовые Sprite-экспорты hand/arrow_left_right/restart в кадре давали белые прямоугольники. Для UI подключены исходные PNG из Assets/Visual/Texture2D. У трёх PNG настроен Sprite-импорт с прозрачностью без mipmaps; GUID сохранены, сами изображения не изменены.
- Пройдены **24/24 PlayMode-тестов** (`Logs/ValidationProject/Logs/stage3-verified.xml`). Проверены ожидание, первый жест, одна попытка, реальный клик мышью по UI-перезапуску, отсутствие случайного старта при нажатии UI, повторная загрузка единственного префаба, сброс позы/одежды/счёта и терминальные состояния. Сохранены проверки коленей и коротких жестов.
- Тесты цикла используют официальный InputTestFixture из установленного Input System: виртуальное устройство изолировано от буфера редактора. Прежние попытки теста UI с ручной подачей в обычный Editor читали состояние Mouse, но InputAction UI не получали те же события; это ограничение тестовой среды устранено. Производственные настройки фокуса не изменялись.
- Новый общий scope тестов сохраняет и восстанавливает четыре ключа прогресса Level Manager.
- Счёт пока фиксируется в 40; окончательные финиш, экраны результатов и Next относятся к этапу 6. На данном этапе конец тестовой дорожки вызывает Finishing и через 0.8 с Won с танцем. Поражение проверяется через Inspector Editor.
- После просмотра UI размытые увеличенные рамки заменены скруглённой геометрией RoundedPanel. Повторно просмотрен кадр 880×1920 с положением камеры из runtime-настроек; рука, стрелки, текст и перезапуск читаются, подсказка не перекрывает персонажа.
- Итоговая Windows x64 Development-сборка успешна: **0 ошибок, 0 предупреждений**. Аудит: **0 отсутствующих компонентов, 0 материалов без шейдера**. Отчёты: `Logs/ValidationProject/Logs/stage3-release-build.log`, `build-result.txt`, кадр `Logs/ValidationProject/Logs/Stage3/ready.png`.
- После окончательной правки UI повторно пройдены **24/24** тестов: `Logs/ValidationProject/Logs/stage3-release-tests.xml`. В основной проект перенесены проверенные сцена, префаб и метаданные PNG.

## Этап 4 — пикапы и шкала

- Этап 3 принят пользователем и опубликован в коммите `19ae3e5`. Реализован отдельный этап 4; ожидается ручная приёмка.
- Добавлены WealthSettings, PlayerWealth, PickupCollector, TrackPickup, WealthHud и PickupFeedback. Счёт отделён от состояния забега; RunSession разрешает подбор только в Running. Пройденный отрезок проверяется до события завершения трассы; при загрузке уровня список пикапов привязывается заново.
- Пикапы сохранены как вложенные model prefab на основе выданных bills.fbx и bottle.fbx. Меши, импорт и GUID исходных моделей не менялись. Материал Atlas из принятого этапа корректно отображает купюры и бутылку; для частиц созданы два URP-материала.
- Визуальная проверка выявила неверное прежнее соответствие костюмов: buisiness — красный деловой костюм, bling — оранжевая куртка/джинсы, cocktail — золотое платье. Для пяти статусов назначены poor/casual/middle/bling/cocktail. Неиспользуемые меши сохранены в исходном model prefab; исходный FBX не менялся. Ретаргетинг и принятый поворот модельки не изменены.
- Шкала обновляет экранную позицию после камеры, не перехватывает UI-ввод. Эффекты и цифры сбрасываются при перезапуске. Предметы при предельном счёте расходуются, но не дают ложную цифру награды.
- Окончательная версия прошла **28/28 PlayMode-тестов**: `Logs/stage4-final-tests.xml`. Новые проверки охватывают 2/30/60/120 FPS, границы одежды и счёта, соответствие мешей, однократность и защиту от повторного входа через событие, Ready/Finishing/Won/Lost, восстановление предметов и очистку частиц при рестарте. Сохранены проверки коленей, коротких жестов и UI-клика из прошлых этапов.
- Просмотрены кадры `Logs/ValidationProject/Logs/Stage4`: ready, upgrade, decent, loss, rich и варианты исходных костюмов. Они получены из Play Mode в проверочной копии, 880×1920. Захват улучшения использует быстрое прохождение ряда для одновременного просмотра состояния и эффекта; плотность частиц на таком кадре выше обычного одиночного подбора.
- Сцена, префабы, настройки, материалы, код и метаданные перенесены в основной проект и побайтно сопоставлены с проверенной копией. Исходный Level Manager и зависимости не менялись.
- Числа в WealthSettings и текущая расстановка — начальная реконструкция для согласования. Полная расстановка, ворота, флаги и препятствия остаются этапом 5; финиш и игровые условия поражения — этапом 6. Значки +/− над предметами и точная вспышка пока отсутствуют; это учтено в REFERENCE.md для полишинга.
- Итоговая Windows x64 Development-сборка: **0 ошибок, 0 предупреждений** (`Logs/stage4-final-build.log`, `Logs/ValidationProject/Logs/build-result.txt`). Аудит: **0 отсутствующих компонентов, 0 материалов без шейдера**.

### Уточнение поражения после ручной проверки этапа 4

- По требованию пользователя добавлено поражение при попытке снизить счастье строго ниже нуля. RunSession проверяет сумму до ограничения PlayerWealth, затем вызывает существующий Lose: движение останавливается, управление и дальнейший подбор блокируются, проигрывается Stomping. Точный ноль ещё позволяет восстановиться деньгами.
- RunnerMotor не публикует завершение трассы, если обработка пикапа уже остановила движение. Time.timeScale не изменяется: анимация поражения продолжает проигрываться.
- Пройдены **29/29 PlayMode-тестов** (`Logs/stage4-defeat-tests.xml`). Проверены поражение от третьей бутылки при начальном счёте 40, переход Animator в Base Layer.Defeat, неподвижность после поражения, точный ноль, восстановление, потеря при уже нулевом счёте, блокировка дальнейших изменений и рестарт. Сборка из предыдущей проверки не повторялась; текущие изменения скомпилированы и проверены в Play Mode.

## Этап 5 — первый маршрут, выбор и флаги

- Этап 4 принят пользователем и опубликован в коммите `3f55341`. Реализован отдельный этап 5; ожидается ручная приёмка.
- Level01 содержит четыре прямых и три дуги, 70 пачек денег, 14 бутылок, парные ворота и три жёлтые зоны с поднимающимися флагами. Маршрут и объекты сохранены в префабе; новый FirstLevelTrack.asset подготовлен Editor-инструментом, а не генерируется при старте.
- Использованы выданные Mesh-ассеты Flag, ChoiceDoor, Party, Hat_School и Diplome. Для рамы ворот, выбранной стороны и жёлтой зоны созданы материалы совместимого шейдера Reference Lit. Этикетка бутылки развёрнута к игроку изменением вложенного префаба; исходный FBX и его GUID не изменены.
- TrackInteraction хранит положение и защиту от повторного срабатывания для TrackPickup, ChoiceGate и FlagZone. Сериализованные имена distance/lateralOffset и GUID TrackPickup сохранены при переносе полей в базовый класс. PickupCollector связывает эти объекты одним списком, отсортированным по расстоянию: большие шаги движения не обрабатывают деньги за воротами раньше выбора.
- ChoiceGate использует интерполированное боковое положение в момент пересечения. Выбирается только одна сторона; при точном центре — школа. FlagZone поднимает исходные меши один раз, не меняя скорость или счёт. При поражении дальнейшие взаимодействия в том же кадре прекращаются.
- Пройдены **33/33 PlayMode-теста** (`Logs/stage5-tests.xml`). Добавлены контрольный проход с итогом 96 при 2/30/60/120 FPS, обе стороны ворот, пересечение ворот при смене стороны в одном кадре, однократность и сброс выбора, подъём и сброс флагов, блокировка денег за смертельным выбором в том же кадре.
- Проверки экономики отделены от конкретной расстановки первого уровня: тесты шкалы используют небольшой набор предметов, создаваемый только в тестах. Отдельные новые тесты проверяют настоящую расстановку Gameplay и её восстановление при перезапуске. Тесты цикла сессии временно выключают сбор предметов, чтобы проверять завершение независимо от маршрута обхода бутылок.
- Исходный Level Manager, версия Unity, пакеты, настройки управления, ретаргетинг и принятые анимации не изменены. Шахматная линия, x2–x5, фотограф, экраны результатов и Next остаются этапом 6.

- Просмотрены ключевые кадры 880×1920 в `Logs/ValidationProject/Logs/Stage5`. Исправлена ориентация бутылок и значка Party; добавлены серые вставки табличек с более крупными надписями. Школа и вечеринка используют выданные 3D-значки. Захват перемещает персонажа между контрольными точками и отдельно вызывает состояния для визуальной проверки; реальную проходимость проверяет FirstLevelTests.
- Окончательная Windows x64 Development-сборка завершена с **0 ошибками и 0 предупреждениями**; аудит — **0 отсутствующих компонентов и 0 материалов без шейдера**. Отчёты: `Logs/stage5-release.log`, `Logs/ValidationProject/Logs/build-result.txt`, `project-validation.json`.
- В основной проект перенесены окончательные сцена, Level01, префабы ворот/флагов/бутылки, меш трассы и четыре материала вместе с метаданными. Этап 5 не коммитился: ожидается ручная приёмка.
- После окончательной правки оформления повторно пройдены **33/33 PlayMode-теста** (`Logs/stage5-release-tests.xml`). Код, сцена, префабы, материалы, настройки и метаданные основного проекта побайтно совпали с проверенной копией.

### Стартовая походка после ручной проверки этапа 5

- По просьбе пользователя стартовая походка сделана слегка грустной: Happy Score = 100, стартовое Happiness = 0.4 (20% Sad Walk / 80% Walking). Счёт, пороги одежды и скорость движения не изменены.
- При ResetValue параметр Animator устанавливается сразу, чтобы старт и рестарт не начинались с прежнего обычного или счастливого смешивания. При подборе денег/алкоголя сохранено сглаживание.
- Пройдены **33/33 PlayMode-теста** (`Logs/stage5-start-gait-tests.xml`), включая начальное Happiness и немедленный сброс параметра Animator после рестарта. Сборка после этой настройки не повторялась; степень грусти ожидает визуальной оценки пользователем.

### Усиленная стартовая грусть

- После обратной связи Happy Score увеличен со 100 до 150 в настройках и значении по умолчанию. Стартовое Happiness = 40 / 150, примерно 47% Sad Walk; текущий счёт и пороги одежды сохранены.
- Пройдены 3/3 RunSessionTests в PlayMode (`Logs/stage5-stronger-gait-tests.xml`): проверены старт и немедленный сброс параметра Animator после рестарта. Визуальная оценка усиленной грусти оставлена пользователю.

- Пользователь подтвердил этап 5 и усиленную стартовую грусть; разрешены коммит, push и переход к этапу 6.

## Этап 6 — финиш и следующий уровень

- Этап 5 принят, закоммичен и отправлен в main: `cafa6b5`.
- Основная расстановка сохранена, последняя прямая продлена на 36 единиц. Добавлены шахматная линия, ворота x2–x5 из исходных мешей, семь купюр, две бутылки, ковры, скамейки, мусор и исходный photographer с Arm Stretching. Для створок подключена door_texture, созданы URP-совместимые материалы финиша.
- Финальные ворота обрабатываются в порядке пересечения вместе с другими взаимодействиями. При недостаточном счёте RunnerMotor останавливается на границе, даже при пересечении нескольких объектов за кадр. После остановки запущены Hip Hop Dancing и результат. Ниже нуля сохранено поражение со Stomping; Retry появляется с небольшой задержкой, не останавливающей Animator.
- RunLevelSource вызывает имеющийся LevelManager.NextLevel и сохраняет прогресс. Защищены повторные Next/Restart в одном кадре, сбрасываются состояние, внешний вид, счёт, камера, пикапы и ворота. Исходный код Level Manager на этом этапе не изменялся.
- Добавлены RunResultHud, FinishGate, FinishCourse и явный Editor-инструмент FinishStageBuilder. Камера плавно отъезжает у финиша; пройденные рамы скрываются, чтобы не перекрывать кадр. Настройки основной трассы сохранены и восстановлены при рестарте/Next.
- Пройдены **37/37 PlayMode-тестов** (`Logs/stage6-final-tests.xml`): пороги всех ворот, большой шаг движения, контрольный результат 440 при 2/30/60/120 FPS, блокировка действий после завершения, танец/поражение, повторный клик Next/Retry, один экземпляр уровня, восстановление камеры и прогресс после перезагрузки сцены.
- Финальная Windows x64 Development-сборка успешна: **0 ошибок, 0 предупреждений** (`Logs/stage6-release.log`, `Logs/ValidationProject/Logs/build-result.txt`). Аудит: **0 отсутствующих компонентов, 0 материалов без шейдера**.
- Просмотрены захваты финиша и UI 880×1920 (`Logs/ValidationProject/Logs/Stage6`). По ним исправлено кадрирование ворот и фотографа и устранено перекрытие пройденными рамами. Захваты используют контролируемое перемещение для визуальной проверки; реальный путь и состояния отдельно проверены тестами.
- Проверенные сцена, Level01, геометрия, префабы и материалы перенесены в основной проект вместе с GUID. Все файлы Assets/_Project совпали с проверочной копией. Код и Markdown прошли проверку пробелов; Unity YAML содержит автоматически записанные пустые значения с завершающим пробелом.
- Остаются отличия от видео в одежде фотографа, части декора и оформлении результата; рекламная рулетка и мета не входят в объём. Touch на устройстве не проверен. Этап 6 ожидает ручной приёмки; его коммит не выполнен.

### Доработка финиша по ручной проверке

- Последним воротам добавлен явный признак Is Final. При любом счёте Runner останавливается перед ними; x5 при достижении порога сохраняется, но створки не открываются. Остановка на границе работает и при большом шаге кадра.
- За воротами добавлены ещё 10 единиц дорожки и 168 декоративных пачек из исходного bills.fbx, без компонентов подбора. Персонаж до них не доходит.
- Удалено отключение визуала пройденных ворот. Камера финиша перенастроена на offset (0, 1.9, −6), pitch 12.5°, FOV 60°: предыдущая рама находится позади камеры при финальной остановке. Основная камера трассы сохранена.
- Пройдены **38/38 PlayMode-тестов** (`Logs/stage6-final-gate-tests.xml`). Проверены все границы счёта, остановка у x5 при 140/150, сохранение видимости ворот, отсутствие подбора у кучи денег, результат 750 при максимальном счёте, прежний маршрут с результатом 440, Next/Retry и прогресс.
- Просмотрены обновлённые кадры x3 и максимального результата (`Logs/ValidationProject/Logs/Stage6/maximum-score-finish.png`): закрытые створки, танец и деньги за решёткой видны; предыдущая рама не перекрывает кадр. Захваты остаются постановочной проверкой представления, реальные переходы проверены тестами.
- Проверенные сцена, меш дорожки и префабы перенесены в основной проект с сохранением GUID. Assets/_Project побайтно совпал с проверочной копией. Отдельная сборка после этой доработки не повторялась; компиляция и PlayMode проверены. Доработка ожидает ручной приёмки, коммит не выполнен.

- Пользователь принял доработку этапа 6 и разрешил коммит, push и переход к этапу 7.
